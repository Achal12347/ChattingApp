rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for all users (completely open database)
    match /{document=**} {
      allow read, write, create, delete, list: if true;
    }
  }
}

